# Exemplos do uso de array dentro da linguagem
# É preciso importar o array, está na biblioteca padrão da linguagem
# Seu uso deve ser evitado, o exemplo é apenas por curiosidade
import array as arr

# Instanciar e informar o tipo do array
# Um array por padrão, só pode armazenar um tipo de dado
# d = decimal
arr.array("d", [1, 3.5])
